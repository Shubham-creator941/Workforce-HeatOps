# Recommended GitHub settings

Protect `main` after CI has run successfully at least once. Require a pull request, one approving review (two for safety-critical changes), resolution of conversations, and passing JavaScript/TypeScript and Python CI jobs. Do not allow force pushes or deletion. Grant an emergency bypass only to trusted maintainers so the team cannot be locked out.
