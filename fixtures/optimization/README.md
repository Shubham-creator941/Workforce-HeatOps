# Optimization fixtures

`continuous_work_plan.json` is a minimal version 1.0 batch request. Its synthetic upstream safety decisions authorize two 15-minute slots for one required task. The expected result is `OPTIMAL`, task `wall` on `crew-a` in `north`, occupying indices `[0, 2)` with both evidence references preserved. This is a contract fixture, not a scientific exposure measurement or safety recommendation.
